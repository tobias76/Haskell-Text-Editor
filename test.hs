import Data.List
import System.IO

leftSequence = ("L")
rightSequence = ("R")
highlightSequence = ("H")
bufferSequence = ("B")

textEditor = [leftSequence, rightSequence, highlightSequence, bufferSequence]
